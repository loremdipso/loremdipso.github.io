<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Posts on Welcome weary traveler</title><link>https://loremdipso.com/posts/</link><description>Recent content in Posts on Welcome weary traveler</description><generator>Hugo -- gohugo.io</generator><language>en</language><lastBuildDate>Thu, 27 Jan 2022 00:00:00 +0000</lastBuildDate><atom:link href="/posts/index.xml" rel="self" type="application/rss+xml"/><item><title>Secret Santa: a Saga</title><link>/posts/secret-santa/</link><pubDate>Thu, 27 Jan 2022 00:00:00 +0000</pubDate><guid>/posts/secret-santa/</guid><description>The end result Before you commit to reading this thing, here&amp;rsquo;re the final versions of this app: Svelte, React. In future I&amp;rsquo;ll only be updating the Svelte version.
Backstory I have a groups of friends from college that have a Secret Santa event every year. Even after we all moved apart we&amp;rsquo;ve kept it up, jumping to Zoom to unwrap our gifts and guess who was our gifter. One of my most favorite traditions, a delight tempered only by the daunting task of logistics.</description><content type="html"><![CDATA[<p><img src="https://loremdipso.com/secret_santa/santa.svg" alt="Santa!" title="OMG IT'S SANTA"></p>
<h3 id="the-end-result">The end result</h3>
<p>Before you commit to reading this thing, here&rsquo;re the final versions of this app: <a href="https://loremdipso.com/secret_santa/">Svelte</a>, <a href="https://loremdipso.com/secret_santa_react/">React</a>. In future I&rsquo;ll only be updating the Svelte version.</p>
<h3 id="backstory">Backstory</h3>
<p>I have a groups of friends from college that have a <a href="https://en.wikipedia.org/wiki/Secret_Santa">Secret Santa</a> event every year. Even after we all moved apart we&rsquo;ve kept it up, jumping to Zoom to unwrap our gifts and guess who was our gifter. One of my most favorite traditions, a delight tempered only by the daunting task of logistics. You see, there are some constraints:</p>
<ol>
<li>Couples are already going to give each other gifts so they shouldn&rsquo;t be each other&rsquo;s Secret Santa</li>
<li>Secret Santa gifter/giftee pairs shouldn&rsquo;t be repeated (if possible). If not possible then we should prefer repeats that are as chronologically separate as possible. That is, a repeat from five years ago is much better than one from last year.</li>
<li>No one should know their Secret Santa, including the person doing the organizing.</li>
</ol>
<h3 id="initial-approach">Initial approach</h3>
<p>At first I wrote a simple ruby script that would generate text files whose names were the gifter and whose contents were the giftee. The organizer would then send an email out to everyone, attaching the correct file to each email. This worked, but was relatively inflexible. Plus previous years didn&rsquo;t automatically carry over, so the organizer would have to</p>
<h3 id="existing-solutions">Existing solutions</h3>
<p>The closest thing I found out in the wild that didn&rsquo;t collect any info or show any ads was <a href="https://arcanis.github.io/secretsanta/">https://arcanis.github.io/secretsanta/</a>. It wasn&rsquo;t exactly what I was looking for, plus the exclusions seemed a bit broken, but it had the fantastic idea of encoding the giftee&rsquo;s information in the URL, using AES with a static key as an obfuscator. Neat!</p>
<h3 id="the-classic-excuse">The classic excuse</h3>
<p>If I&rsquo;m being honest with myself I could have pretty easily extended the ruby script to do everything a web app could, but I&rsquo;m always looking for any reason to try out a new technology. This time it was <a href="https://svelte.dev/">Svelte</a>. I&rsquo;d heard it has Vue-like structure and Syntax, Vanilla JavaScript performance, and I&rsquo;d already <a href="https://loremdipso.com/tags/lithe/">started writing a compiler for it</a>. So let&rsquo;s give a go, shall we?</p>
<h3 id="ui">UI</h3>
<p>The one thing anyone who uses Svelte seems willing to criticize is the lack of ecosystem. However I was able to find a <a href="https://sveltematerialui.com/">SMUI</a>, a Svelte implementation of <a href="https://material.io/">Material Design</a> that seemed reasonable enough.</p>
<h3 id="dev-experience">Dev experience</h3>
<p>Here were some initial thoughts and minor complaints:</p>
<ol>
<li>
<p>Setup was easy enough with a good project template.</p>
</li>
<li>
<p>Webpack continues to be terribly complex, so for this project I finally gave <a href="https://rollupjs.org">rollup</a> a shot. It kind of seemed like a Webpack with less historical baggage, which is exactly what I wanted.</p>
</li>
<li>
<p>Svelte&rsquo;s component-specific styles are very nice.</p>
</li>
<li>
<p>Svelte&rsquo;s hot reloading: also very nice</p>
</li>
<li>
<p>Wow, I really like those <code>$</code>. I&rsquo;ve written enough using React&rsquo;s hooks that I&rsquo;m never confident my code&rsquo;s going to be fast unless I memoize everything, so it&rsquo;s great to see something that just works.</p>
</li>
<li>
<p>At first Svelte&rsquo;s loud complaints about unused styles got on my nerves, but I&rsquo;ve come to accept that they&rsquo;re probably for the best.</p>
</li>
<li>
<p>I don&rsquo;t think my app was complex enough to really take advantage of Svelte&rsquo;s proclaimed runtime performance benefits, but I have no complaints.</p>
</li>
<li>
<p>I noticed a couple of minor consistency issues. If I had a bug in a <code>.ts</code> file that wasn&rsquo;t used anywhere, and I renamed it so it was a <code>.txt</code> just to get rid of the errors, Rollup or Svelte or TSC or whoever should notice did not. I had to restart the dev server for it to see that change.</p>
</li>
</ol>
<h3 id="bundle-size">Bundle size</h3>
<p>I was a little disappointed that the Svelte-based Material UI library spit out <strong>500 KB</strong> just in styles. I&rsquo;d love to get rid of most of that, but even so the whole project comes in at under <strong>1 MB</strong> total. Okay, but how does it compare to, say, React? Well, luckily I first wrote this app using React and <a href="https://ant.design/">Antd</a>. The implementations aren&rsquo;t exactly the same but they have nearly the same set of features. All told, the build of the React version comes out at around <strong>3.4 MB</strong>, which seems awfully high to me, even for React. After some digging it turns out it was due to that version of antd having <a href="https://stackoverflow.com/questions/48721290/ant-design-huge-imports">some issues</a>, so if I just <code>npm update</code> and rebuild I get&hellip;</p>
<p><strong>11 MB</strong> ?!?! Whoa whoa whoa, that can&rsquo;t be right. Alright, what if we use <a href="https://github.com/umijs/babel-plugin-import">babel-plugin-import</a>, so that imports like <code>import { Button } from 'antd';</code> automatically turn into something more like <code>import Button from 'antd/lib/button';</code>. Apparently that&rsquo;s good for tree shaking. Aaaand&hellip; I actually kind of hate babel, so let&rsquo;s try and avoid it by just manually updating these imports. Aaaand&hellip;</p>
<p><strong>7.7 MB</strong>. Wellllll, that&rsquo;s&hellip; better, at least, but something&rsquo;s definitely squiffy. Alright, fine, I&rsquo;ll use babel. Aaaand&hellip; no change. Darn. Oh, wait! Looks like the real space-taker are <a href="https://github.com/ant-design/ant-design/issues/12011#issuecomment-623043192">the icons</a>, and we should use babel for that too. And with that I&rsquo;m down to&hellip; <strong>4.8MB</strong>! <em>sigh</em> Alright, at this point I&rsquo;ve had a gut full and just want to try something new.</p>
<h3 id="meanwhile-in-svelte-land">Meanwhile, in Svelte land&hellip;</h3>
<p>So <strong>1 MB</strong>, huh? Alright, just in case I was doing something silly let&rsquo;s go ahead and just try to use a <a href="https://carbon-components-svelte.onrender.com/">similar UI library</a> in an app built using <a href="https://kit.svelte.dev/">Svelte Kit</a>&rsquo;s starter app template. That should give me the latest and greatest setup. But no, that also ended up costing ~500KB in styles, base, before I even started to pull in components. Why am I paying for junk I&rsquo;m not even using? Hopefully users of that lib have a way to cut those out, but for the life of me I haven&rsquo;t been able to find it.</p>
<h3 id="enter-smelte">Enter Smelte</h3>
<p><a href="https://smeltejs.com/">Smelte</a>. The elevator pitch is basically <a href="https://material.io/">Material Design</a> + <a href="https://tailwindcss.com/">Tailwind CSS</a>. You can use all of the normal Tailwind styles in your app plus a tonne of pre-built components. There was one outstanding issue where Smelte doesn&rsquo;t currently list a specific version of purgecss and is slightly incompatible with it. Hopefully that PR gets accepted soon, but until then I had great luck using an older version of <a href="https://www.npmjs.com/package/purgecss">purgecss</a>.</p>
<p>So now that I&rsquo;ve got my UI framework swapped out and patched up and worked like a dream, let&rsquo;s see the build size&hellip; <strong>200 KB</strong>!!! Alright, now that&rsquo;s more like it.</p>
<h3 id="caveats-and-conclusions">Caveats and Conclusions</h3>
<p>I should clarify that comparing the React+Carbon app against the Svelte+Smelte app isn&rsquo;t entirely fair. For one I just gave up on React+Carbon after a certain point. I&rsquo;d hope that production apps aren&rsquo;t shipping with a framework that&rsquo;s got a <strong>4 MB</strong> &ldquo;Hello World&rdquo;. Also, Carbon embeds its icons as SVG directly in its final bundle, whereas I reference Google Material Icons font library as an external URL. So to be more fair I should either increase the Svelte app&rsquo;s total size or decrease the React app&rsquo;s total size.</p>
<p>That said, I don&rsquo;t think I can definitively claim something as sweeping as &ldquo;Svelte &gt; React&rdquo; or &ldquo;Smelte &gt; Carbon&rdquo;. What I can say is that testing performance is, as usual, important and <em>hard</em>. Even after deciding that bundle size was the metric I wanted to improve, actually going about improving it meant a lot of trial and error. I learned a lot, but if this project wasn&rsquo;t for fun I definitely wouldn&rsquo;t have put this much time or effort into it. And in the end I did find a combination of technologies that I&rsquo;m really happy with that I&rsquo;d love to try out in future projects, so I&rsquo;ll chalk this one up as smashing success.</p>
<p>Please tease me if I spend any more time re-writing this silly app again.</p>
<details>
  <summary>
	Bonus: 2048
  </summary>
  <p>
	I did this same exercise with another of my old projects: <a target="#" href="https://loremdipso.com/YAN2048">YAN2048</a>. This one was written in Angular and only used its UI framework for the buttons and a game-over dialog. The <a target="#" href="https://loremdipso.com/YAN2048_Svelte">clone</a> does the same, and I copy/pasted very nearly all the same logic and styles, yielding two very similar apps.
  </p>
  <h4>
	Results
  </h4>
  <p>
	On-disk space, Angular: an even <b>500 KB</b>, Svelte: <b>84 KB</b>.
  </p>
  <p>
	Total delivered to browser (according to my network tab), Angular: <b>587 KB</b> inflated (<b>251 KB</b> with gzip), Svelte: <b>224 KB</b> inflated (<b>179 KB</b> with gzip).
  </p>
  <p>
	But is this comparison worthwhile? Is what I observed experimentally vaguely in line with what they should be? Well, according to <a href="https://github.com/LayZeeDK/ngx-ivy-minimal-app-with-ngzone">this git repo</a>, a hello world using the <a href="https://docs.angular.lat/guide/ivy">Ivy renderer</a> costs around <b>131 KB</b> uncompressed. So, yeah, I think the results here are reasonable.
</details>
]]></content></item><item><title>Lithe 7 - Apples to Apples</title><link>/posts/lithe-7/</link><pubDate>Sat, 15 Jan 2022 12:46:47 -0700</pubDate><guid>/posts/lithe-7/</guid><description>Confession time My performance testing methodology so far has been extremely flawed. It serves mostly as an indicator for how rigorous performance testing might go when I eventually get around to it. Which I think is reasonable, but it doesn&amp;rsquo;t have to be quite so unfair to Svelte. Svelte is doing quite a lot more than Lithe, so I really shouldn&amp;rsquo;t be comparing them side by side. It&amp;rsquo;d be good to re-run some earlier tests, but with the JavaScript equivalent of what Lithe is doing.</description><content type="html"><![CDATA[<h3 id="confession-time">Confession time</h3>
<p>My performance testing methodology so far has been extremely flawed. It serves mostly as an indicator for how rigorous performance testing might go when I eventually get around to it. Which I think is reasonable, but it doesn&rsquo;t have to be quite so unfair to Svelte. Svelte is doing quite a lot more than Lithe, so I really shouldn&rsquo;t be comparing them side by side. It&rsquo;d be good to re-run some earlier tests, but with the JavaScript equivalent of what Lithe is doing. Which, let&rsquo;s be honest, is mostly just parsing the HTML into an AST.</p>
<h3 id="disambiguation">Disambiguation</h3>
<p>It&rsquo;s good to have names for things, so let&rsquo;s call this super-basic JavaScript version <a href="https://en.wikipedia.org/wiki/Tongue-in-cheek">LitheJS</a>.</p>
<h3 id="the-specifics">The specifics</h3>
<p>Lithe has been using the <a href="https://crates.io/crates/html_parser">html_parser crate</a>. For LitheJS I&rsquo;ll use the very popular <a href="https://www.npmjs.com/package/node-html-parser">Fast HTML Parser</a>. It&rsquo;s got 2.3M downloads a week, and it&rsquo;s even got Fast in the name. If anything&rsquo;s going to give Lithe a run for its money it&rsquo;s going to be that.</p>
<h3 id="the-methodology">The methodology</h3>
<p>We&rsquo;ll do the same as before: super simple HTML files, just N <code>&lt;span&gt;Hello world!&lt;/span&gt;</code> elements. No nesting, no scripting, and I&rsquo;m not even going to ask LitheJS to produce any output or perform any transformations. I just want to know: how long does it take to parse the HTML into an AST, and how does that compare with Lithe?</p>
<p>My thought is if Lithe can still beat out LitheJS, even after giving it all these advantages, then we&rsquo;re on the right track.</p>
<p>Notably, though, I&rsquo;m not going to bother improving any of the rest of my methodology just yet. I&rsquo;m not averaging multiple runs, I&rsquo;m not going to nest HTML elements, I&rsquo;m measuring performance directly in Node, all results are in ms, etc.</p>
<h3 id="the-results">The results</h3>
<table>
<thead>
<tr>
<th>Number of <code>span</code>s</th>
<th>Svelte</th>
<th>Lithe - native</th>
<th>Lithe - WASM</th>
<th>LitheJS</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>7ms</td>
<td>3ms</td>
<td>1ms</td>
<td>1ms</td>
</tr>
<tr>
<td>10</td>
<td>10ms</td>
<td>1ms</td>
<td>1ms</td>
<td>1ms</td>
</tr>
<tr>
<td>100</td>
<td>108ms</td>
<td>4ms</td>
<td>3ms</td>
<td>1ms</td>
</tr>
<tr>
<td>1000</td>
<td>348ms</td>
<td>6ms</td>
<td>9ms</td>
<td>3ms</td>
</tr>
<tr>
<td>10,000</td>
<td>N/A (<a href="https://en.wikipedia.org/wiki/Stack_buffer_overflow">SO</a>)</td>
<td>53ms</td>
<td>81ms</td>
<td>125ms</td>
</tr>
</tbody>
</table>
<p>Wow, that Fast HTML Parser really is fast! At least until you get into really big files. But let&rsquo;s dig into that a bit more. Right now I&rsquo;m just parsing these file contents and throwing away the result:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-ts" data-lang="ts"><span class="kd">function</span> <span class="nx">simple_html_parser</span><span class="p">(</span><span class="nx">contents</span>: <span class="kt">String</span><span class="p">)</span> <span class="p">{</span>
	<span class="kr">const</span> <span class="nx">root</span> <span class="o">=</span> <span class="nx">parse</span><span class="p">(</span><span class="nx">contents</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div><p>I haven&rsquo;t read the source, but I suppose it&rsquo;s possible that Fast HTML Parser is being super <a href="https://en.wikipedia.org/wiki/Lazy_evaluation">lazy</a> and not fully parsing the HTML string it was given until it has to. Let&rsquo;s make it work a little harder:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-ts" data-lang="ts"><span class="kd">function</span> <span class="nx">simple_html_parser</span><span class="p">(</span><span class="nx">contents</span>: <span class="kt">String</span><span class="p">)</span> <span class="p">{</span>
	<span class="kr">const</span> <span class="nx">root</span> <span class="o">=</span> <span class="nx">parse</span><span class="p">(</span><span class="nx">contents</span><span class="p">);</span>
	<span class="kr">const</span> <span class="nx">result</span> <span class="o">=</span> <span class="nx">contents</span><span class="p">.</span><span class="nx">toString</span><span class="p">();</span>
	<span class="k">return</span> <span class="nx">result</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div><p>Let&rsquo;s re-run those last couple of tests. I included a range here since the results were highly variable:</p>
<table>
<thead>
<tr>
<th>Number of <code>span</code>s</th>
<th>LitheJS</th>
</tr>
</thead>
<tbody>
<tr>
<td>1000</td>
<td>8ms-18ms</td>
</tr>
<tr>
<td>10,000</td>
<td>58ms-140ms</td>
</tr>
</tbody>
</table>
<p>Alright, so it&rsquo;s a little bit slower, but not by that much. I&rsquo;m guessing there&rsquo;s no lazy evaluation going on here: Fast HTML Parser is just fast. For fun I added timing inside the native version of Lithe just around the HTML parsing bit. And if we get rid of the <code>toString()</code> call in our <code>simple_html_parser</code> we can compare just the DOM parsing.</p>
<table>
<thead>
<tr>
<th>Number of <code>span</code>s</th>
<th>LitheJS (just HTML parsing)</th>
<th>Lithe - native (just HTML parsing)</th>
</tr>
</thead>
<tbody>
<tr>
<td>100</td>
<td>1ms</td>
<td>0ms</td>
</tr>
<tr>
<td>1000</td>
<td>4ms-13ms</td>
<td>1ms</td>
</tr>
<tr>
<td>10,000</td>
<td>41ms-142ms</td>
<td>18ms-27ms</td>
</tr>
</tbody>
</table>
<p>And with that we&rsquo;re finally comparing like with like. And the results look how I&rsquo;d expect. If we take out the FFI overhead and only parse the HTML then the Rust version is looking to be a bit faster, especially for larger files.</p>
]]></content></item><item><title>Lithe 6: What about WASM?</title><link>/posts/lithe-6/</link><pubDate>Thu, 13 Jan 2022 00:00:00 +0000</pubDate><guid>/posts/lithe-6/</guid><description>Excuses I know I haven&amp;rsquo;t talked much about the state of Lithe except to mention how fast it is while run natively, but that&amp;rsquo;s only because Lithe is still very very bare bones. It parses the contents of svelte files into an HTML AST, which it then transforms into a more Svelte-y AST, which it then runs through to generate the final output. And it only works for the simplest of svelte files.</description><content type="html"><![CDATA[<h3 id="excuses">Excuses</h3>
<p>I know I haven&rsquo;t talked much about the state of Lithe except to mention how fast it is while run natively, but that&rsquo;s only because Lithe is still very very bare bones. It parses the contents of svelte files into an HTML AST, which it then transforms into a more Svelte-y AST, which it then runs through to generate the final output. And it only works for the simplest of svelte files.</p>
<h3 id="thinking-ahead">Thinking ahead</h3>
<p>But before I write more about the internals of Lithe or implement any new features, I thought it&rsquo;d be a fun diversion to think ahead to when this is a smash success and absolutely everyone wants it. How are we going to package and release it? The obvious (and my initial) answer was as a native extension that we talk to via FFI. But how will that be received? JavaScript developers like fast build times, even at the very start. Projects like <a href="https://www.snowpack.dev/">Snowpack</a> are successful exactly because of that. No frontend engineer wants to wait for the latest compiler to be downloaded and build on their local machine, especially if that requires the Rust toolchain.</p>
<h3 id="an-aside">An aside</h3>
<p>I never knew, but apparently tsc is, itself, written in TypeScript. Which really calls into question this whole endeavor. If something as successful as tsc (which I never felt was all that slow) is written in TypeScript, why can&rsquo;t Svelte? The move to Rust has never been a sure one, and this might just be another reason against. Perhaps the performance limitations I&rsquo;m trying to solve are more algorithmic than they are technical.</p>
<h3 id="whats-wasm-again">What&rsquo;s WASM again?</h3>
<p>In any case, assuming Lithe is alive and well and we want to release it to impatient developers, we could maybe release native binaries. This seems (to me, anyway) to be something of a hassle, plus your consumers need to have the utmost trust in you. So what about <a href="https://webassembly.org/">WASM</a>? It&rsquo;s cross-platform, I assume Node can run it, and I can only imagine that it&rsquo;s cutoff from anything dangerous like network or file IO by default.</p>
<h3 id="how-to">How to</h3>
<p>I set to work slightly modifying Lithe to output a WASM module rather than a native Rust lib. There were only two non-obvious changes I had to make. First, I needed to set my WASM&rsquo;s <code>package.json</code>&rsquo;s <code>type</code> to be <code>module</code> to get it to load correctly:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="p">{</span>
  <span class="s2">&#34;name&#34;</span><span class="o">:</span> <span class="s2">&#34;lithe&#34;</span><span class="p">,</span>
  <span class="s2">&#34;version&#34;</span><span class="o">:</span> <span class="s2">&#34;0.1.0&#34;</span><span class="p">,</span>
  <span class="s2">&#34;files&#34;</span><span class="o">:</span> <span class="p">[</span>
    <span class="s2">&#34;lithe_bg.wasm&#34;</span><span class="p">,</span>
    <span class="s2">&#34;lithe.js&#34;</span><span class="p">,</span>
    <span class="s2">&#34;lithe_bg.js&#34;</span><span class="p">,</span>
    <span class="s2">&#34;lithe.d.ts&#34;</span>
  <span class="p">],</span>
  <span class="s2">&#34;module&#34;</span><span class="o">:</span> <span class="s2">&#34;lithe.js&#34;</span><span class="p">,</span>
  <span class="s2">&#34;types&#34;</span><span class="o">:</span> <span class="s2">&#34;lithe.d.ts&#34;</span><span class="p">,</span>
  <span class="s2">&#34;sideEffects&#34;</span><span class="o">:</span> <span class="kc">false</span>
  <span class="s2">&#34;type&#34;</span><span class="o">:</span> <span class="s2">&#34;module&#34;</span><span class="p">,</span> <span class="c1">// &lt;- new
</span><span class="c1"></span><span class="p">}</span>
</code></pre></div><p>I also had to refrain from using <a href="https://doc.rust-lang.org/std/time/index.html"><code>std::time</code></a>, though luckily I was only using that for detailed performance tracing. With those two small changes done it just&hellip; worked. I am now able to import my compiler with a wondrously simple:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-ts" data-lang="ts"><span class="kr">import</span> <span class="p">{</span> <span class="nx">wasm_compile</span> <span class="p">}</span> <span class="kr">from</span> <span class="s2">&#34;./wasm/lithe.js&#34;</span><span class="p">;</span>
</code></pre></div><p>Wow. And the timings were better than I could have hoped: just <code>4ms</code> for 1000 <code>span</code>&rsquo;s, which is just as fast as native!!!</p>
<p>I checked my enthusiasm, and then I checked the output to make sure it was actually doing what it should (it was). Then to make sure I was actually doing the timing correctly (I was) I tried 10,000 <code>span</code>&rsquo;s, and that took only <code>42ms</code>. Svelte, for comparison, had long since overflowed its stack.</p>
<p>I should note that I don&rsquo;t actually think WASM is just as fast as native. Maybe it is in some instances, but the times involved here are so small and the testing itself so basic that I can&rsquo;t confidently prove anything. But what I can do is predict that, when all is said and done, that the WASM version of Lithe is probably going to run about as fast as the native version most of the time. But even if it&rsquo;s 20x slower that&rsquo;s still far faster than Svelte&rsquo;s TypeScript compiler and that&rsquo;s good enough for me.</p>
<h3 id="conclusion">Conclusion</h3>
<p>It might be too early to tell, but just from this it&rsquo;s looking like releasing this compiler via WASM might be totally viable. And the icing on the cake? The current version of Lithe, when compiled to WASM, weighs in at a mere <code>268K</code>. <code>268K</code>! That&rsquo;s even smaller than a native <a href="https://stackoverflow.com/questions/29008127/why-are-rust-executables-so-huge">hello world in Rust</a>. Color me impressed.</p>
<details>
  <summary>
	Disclaimer
  </summary>
  <p>
	I did have to enable node's <code>--experimental-wasm-modules</code> flag to get my WASM binary to even load. I'm hoping that flag goes away and WASM is supported by default, but I suppose there's a chance WASM goes the <a href="https://www.zdnet.com/article/flash-is-dead-long-live-html5/">way of flash</a>.
  </p>
</details>
]]></content></item><item><title>Lithe 5: Optimize for the minifier</title><link>/posts/lithe-5/</link><pubDate>Sat, 08 Jan 2022 19:49:55 -0700</pubDate><guid>/posts/lithe-5/</guid><description>halfnelson did a lovely investigation of how large Svelte projects scale. I&amp;rsquo;m not looking to rehash that per se, but I am interested in how well the current Svelte compiler&amp;rsquo;s output gets minified.
For this investigation I&amp;rsquo;ll be pulling as many .svelte files as I can. Let&amp;rsquo;s use the Autopilot strategy and pull from GitHub repos with permissive enough licenses.
Even doing this was a bit of a project. I figured this might come up again, so I made the code clean enough that I wasn&amp;rsquo;t embarrassed to release it.</description><content type="html"><![CDATA[<p><a href="https://github.com/halfnelson">halfnelson</a> did a lovely <a href="https://github.com/halfnelson/svelte-it-will-scale/blob/master/README.md">investigation of how large Svelte projects scale</a>. I&rsquo;m not looking to rehash that per se, but I am interested in how well the current Svelte compiler&rsquo;s output gets minified.</p>
<p>For this investigation I&rsquo;ll be pulling as many <code>.svelte</code> files as I can. Let&rsquo;s use the <a href="https://copilot.github.com/">Autopilot</a> strategy and pull from GitHub repos with permissive enough licenses.</p>
<p>Even doing this was a bit of a project. I figured this might come up again, so I made the code clean enough that I wasn&rsquo;t embarrassed to release it. You can see what I came up with at the <a href="https://github.com/loremdipso/github_batch_download">github repo</a>. I used Rust with the lovely <a href="https://docs.rs/octocrab/latest/octocrab/">octocrab</a> crate to handle the Github API, <a href="https://docs.rs/git2/latest/git2/">git2</a> to handle downloading the repositories, and a little bit of <a href="https://github.com/tokio-rs/tokio">tokio</a> for async and multi-threading. I&rsquo;m very happy with the result, though I did run into rate-limiting for the GitHub API fairly quickly even with a <a href="https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token">personal access token</a>.</p>
<p>Even so, I was able to come out of that with 220 repos without much trouble, from which I extracted 20MB of raw <code>.svelte</code> files. Once I removed the empties and the duplicates I was left with a respectable <code>18MB</code> worth of files. I ran those files through the svelte compiler, skipping any that wouldn&rsquo;t compile. I didn&rsquo;t expect to run this too often, so I ended up using ruby to call a shell script that told node to compile a particular file with some options, which I ran over every file twice: once with minification and once without. While I ended up with a workflow that took several minutes to run it was the fastest for me to write.</p>
<p>After a cuppa I had this:</p>
<table>
<thead>
<tr>
<th>Type</th>
<th>Total size (KB)</th>
</tr>
</thead>
<tbody>
<tr>
<td>raw <code>.svelte</code> files</td>
<td>18420</td>
</tr>
<tr>
<td>compiled</td>
<td>23188</td>
</tr>
<tr>
<td>minified</td>
<td>15764</td>
</tr>
<tr>
<td>gzipped</td>
<td>10836</td>
</tr>
</tbody>
</table>
<p>Very interestingly there were two files whose minified size was actually slightly larger than the original. This makes me think I could find a better minifier than <a href="https://www.npmjs.com/package/minify">minify</a>. Even after playing with the available options, going so far as to enable some unsafe operations, I still couldn&rsquo;t get that number down. I might try another minifier entirely (<a href="https://www.npmjs.com/package/node-minify">node-minify</a> looks cool), but for now I&rsquo;ll keep what I&rsquo;ve got.</p>
<p>I think that&rsquo;s enough for now. Once I get a compiler going I can use this to measure just how well my output fairs against Svelte&rsquo;s, both raw and after minification. I believe there&rsquo;s some room for improvement if we try and output code that&rsquo;s easy for the minifier to minify. For example, I&rsquo;ve noticed the Svelte compiler outputs something sort of like this for detaching a component (heavily edited for simplicity):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="kr">import</span> <span class="p">{</span> <span class="nx">detach</span> <span class="p">}</span> <span class="nx">from</span> <span class="s2">&#34;./svelte&#34;</span><span class="p">;</span>

<span class="kd">let</span> <span class="nx">a</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">b</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">c</span><span class="p">;</span>

<span class="kd">function</span> <span class="nx">fn</span><span class="p">(</span><span class="nx">do_detach</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">if</span> <span class="p">(</span><span class="nx">do_detach</span><span class="p">)</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">a</span><span class="p">);</span>
	<span class="k">if</span> <span class="p">(</span><span class="nx">do_detach</span><span class="p">)</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">b</span><span class="p">);</span>
	<span class="k">if</span> <span class="p">(</span><span class="nx">do_detach</span><span class="p">)</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">c</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div><p>Which get minified to:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="kr">import</span> <span class="p">{</span> <span class="nx">detach</span> <span class="p">}</span> <span class="nx">from</span> <span class="s2">&#34;./svelte&#34;</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">a</span><span class="p">,</span> <span class="nx">b</span><span class="p">,</span> <span class="nx">c</span><span class="p">;</span>
<span class="kd">function</span> <span class="nx">fn</span><span class="p">(</span><span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
	<span class="nx">t</span> <span class="o">&amp;&amp;</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">a</span><span class="p">),</span> <span class="nx">t</span> <span class="o">&amp;&amp;</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">b</span><span class="p">),</span> <span class="nx">t</span> <span class="o">&amp;&amp;</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">c</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div><p>If, instead, the source was:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="kr">import</span> <span class="p">{</span> <span class="nx">detach</span> <span class="p">}</span> <span class="nx">from</span> <span class="s2">&#34;./svelte&#34;</span><span class="p">;</span>

<span class="kd">let</span> <span class="nx">a</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">b</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">c</span><span class="p">;</span>

<span class="kd">function</span> <span class="nx">fn</span><span class="p">(</span><span class="nx">do_detach</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">if</span> <span class="p">(</span><span class="nx">do_detach</span><span class="p">)</span> <span class="p">{</span>
		<span class="nx">detach</span><span class="p">(</span><span class="nx">a</span><span class="p">);</span>
		<span class="nx">detach</span><span class="p">(</span><span class="nx">b</span><span class="p">);</span>
		<span class="nx">detach</span><span class="p">(</span><span class="nx">c</span><span class="p">);</span>
	<span class="p">}</span>
<span class="p">}</span>
</code></pre></div><p>Which, in this example at least, is functionally equivalent. I haven&rsquo;t dug into Svelte enough to know if this is always going to be a safe transformation. In any case, this can be minified to:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="kr">import</span> <span class="p">{</span> <span class="nx">detach</span> <span class="p">}</span> <span class="nx">from</span> <span class="s2">&#34;./svelte&#34;</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">a</span><span class="p">,</span> <span class="nx">b</span><span class="p">,</span> <span class="nx">c</span><span class="p">;</span>
<span class="kd">function</span> <span class="nx">fn</span><span class="p">(</span><span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
	<span class="nx">t</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="nx">detach</span><span class="p">(</span><span class="nx">a</span><span class="p">),</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">b</span><span class="p">),</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">c</span><span class="p">));</span>
<span class="p">}</span>
</code></pre></div><p>A whole 4 characters saved! I know, I know, it doesn&rsquo;t seem like much, but that&rsquo;s basically free and is a function of how many variables your svelte program has. One last consideration is aliasing. We could, for example, imagine a minifier smart enough to output this:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="kr">import</span> <span class="p">{</span> <span class="nx">detach</span> <span class="nx">as</span> <span class="nx">d</span> <span class="p">}</span> <span class="nx">from</span> <span class="s2">&#34;./svelte&#34;</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">a</span><span class="p">,</span> <span class="nx">b</span><span class="p">,</span> <span class="nx">c</span><span class="p">;</span>
<span class="kd">function</span> <span class="nx">fn</span><span class="p">(</span><span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
	<span class="nx">t</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="nx">d</span><span class="p">(</span><span class="nx">a</span><span class="p">),</span> <span class="nx">d</span><span class="p">(</span><span class="nx">b</span><span class="p">),</span> <span class="nx">d</span><span class="p">(</span><span class="nx">c</span><span class="p">));</span>
<span class="p">}</span>
</code></pre></div><p>We could also eschew aliasing and just have a separate svelte library that only has these minified functions to avoid the &ldquo;as x&rdquo; bit:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="kr">import</span> <span class="p">{</span> <span class="nx">d</span> <span class="p">}</span> <span class="nx">from</span> <span class="s2">&#34;./svelte-mini&#34;</span><span class="p">;</span>
<span class="kd">let</span> <span class="nx">a</span><span class="p">,</span> <span class="nx">b</span><span class="p">,</span> <span class="nx">c</span><span class="p">;</span>
<span class="kd">function</span> <span class="nx">fn</span><span class="p">(</span><span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
	<span class="nx">t</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="nx">d</span><span class="p">(</span><span class="nx">a</span><span class="p">),</span> <span class="nx">d</span><span class="p">(</span><span class="nx">b</span><span class="p">),</span> <span class="nx">d</span><span class="p">(</span><span class="nx">c</span><span class="p">));</span>
<span class="p">}</span>
</code></pre></div><p>The downside of some of these optimizations, of course, is that it becomes harder and harder to debug in production. But if we&rsquo;re already at the point of minifying I say it&rsquo;s <a href="https://en.wikipedia.org/wiki/No_Holds_Barred_(1989_film)">no holds barred</a>.</p>
<details>
  <summary>
	Extra considerations
  </summary>
  <p>
	While minimizing the compiled svelte files seems like an obvious win it may not actually amount to much given how good gzip is. We'll need to run out output through gzip to know if we'll actually be sending fewer bytes down the wire.
  </p>
  <p>
	The examples above are so small that gzip doesn't actually help. The original version is unchanged at 114 bytes while the hand-optimized one is actually larger after compression, growing from 91 to 113 bytes.
  </p>
</details>
]]></content></item><item><title>Lithe 4: On the Shoulders of Giants</title><link>/posts/lithe-4/</link><pubDate>Mon, 03 Jan 2022 18:39:44 -0700</pubDate><guid>/posts/lithe-4/</guid><description>Starting from characters and parsing that into an AST that could render a Svelte program could definitely open open possibilities for some low-level optimizations, but even just parsing a normal HTML document is not a small project, and Svelte includes JavaScript, TypeScript, Handlebars-esque blocks, CSS, and SCSS, too. Doing all that at once would be the mother of all slogs. But, luckily, I don&amp;rsquo;t have to! There exist many, many pre-build alternatives.</description><content type="html"><![CDATA[<p>Starting from characters and parsing that into an AST that could render a Svelte program could definitely open open possibilities for some low-level optimizations, but even just parsing a normal HTML document is not a small project, and Svelte includes JavaScript, TypeScript, Handlebars-esque blocks, CSS, and SCSS, too. Doing all that at once would be the mother of all slogs. But, luckily, I don&rsquo;t have to! There exist many, many pre-build alternatives. The Svelte compiler itself even uses a couple.</p>
<p>So here&rsquo;s the new plan:</p>
<ol>
<li>Create a basic Rust lib and make sure I can get communication working to/from JavaScript via FFI in Node</li>
<li>Send a svelte file as a string through a DOM parser to get a basic AST</li>
<li>Convert that AST to our own custom one
<ul>
<li>This is where we should do any conversions we might want to the original AST, like squashing child Text nodes, splitting nodes or inserting nodes, etc.</li>
</ul>
</li>
<li>Render that custom AST to a string
<ul>
<li>We should eventually keep track of other things, warnings, CSS, etc., but for now we&rsquo;re simply trying to replicate the JavaScript string</li>
<li>We should keep in mind that whatever we do here we&rsquo;ll need to do something very similar when rendering for SSR, so we should do as much AST manipulation as possible before this step. This should be a simple &ldquo;let&rsquo;s render a string&rdquo;</li>
</ul>
</li>
</ol>
<p>But before we really get started let&rsquo;s see what kind of performance we&rsquo;re getting out of the original compiler. I&rsquo;m only looking to test execution times, so to ignore however long it takes to load everything into Node&rsquo;s runtime we&rsquo;ll simply do two runs per test: once to ensure everything&rsquo;s loaded, and the other we&rsquo;ll actually time. How long does Svelte take to compile this simple program?</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
</code></pre></div><p>On my machine this is about <code>5ms</code>. That seems&hellip; high? I think? I haven&rsquo;t written a compiler before, but computers are fast and this program is absolutely tiny. Maybe there&rsquo;s some cost to compile anything, regardless of program size? Let&rsquo;s see how it scales. What if we do 10x?</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">span</span><span class="p">&gt;</span>Hello world!<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span>
</code></pre></div><p>I should note this renders extra calls to a <code>space()</code> function by default (I assume so the resultant DOM looks nice), so to avoid this I remove all newlines during tests. Anyway, with newlines removed this takes <code>16ms</code>. Hmmm&hellip;. let&rsquo;s do a few more:</p>
<table>
<thead>
<tr>
<th>Number of <code>span</code>s</th>
<th>Time to render</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>5ms</td>
</tr>
<tr>
<td>10</td>
<td>16ms</td>
</tr>
<tr>
<td>100</td>
<td>62ms</td>
</tr>
<tr>
<td>1000</td>
<td>432ms</td>
</tr>
</tbody>
</table>
<p>Okay, so half a second for 1000 elements isn&rsquo;t super great, but it&rsquo;s still probably okay. How often are you writing components this big anyhow? And any half-decent system would only recompile whenever you make changes. Still, fast compiles make for happy devs, so this is the mark we&rsquo;re trying to beat.</p>
<p>The only other order of business for now is the name. I did what I always do and looked up some synonyms for similar projects, in this case Svelte. After some browsing I landed on <code>lithe</code>, so that&rsquo;s what I&rsquo;ll use for now until I find some other JS project already picked that name.</p>
<details>
  <summary>
	Click for spoilers
  </summary>
  <p>
	Lithe is far from done, but it can easily render the example programs above. In release mode, 1000 <code>span</code>s takes just <code>5ms</code> to run (!!!), most of which is simply the initial DOM parsing provided by an external library. Time is lost initializing FFI and shuttling strings to/from Node, but even so it turns out Rust is really really fast (who knew?). As we add more features lithe can only slow down, but for now I'll take this as a sign that this work might actually be useful.
  </p>
</details>
]]></content></item><item><title>Lithe 3: A Rewrite</title><link>/posts/lithe-3/</link><pubDate>Mon, 03 Jan 2022 17:07:07 -0700</pubDate><guid>/posts/lithe-3/</guid><description>What if I just copied the Svelte compiler, written in TypeScript, changed all of the file extensions from .ts to .rs, and fixed all the bugs? It&amp;rsquo;d be a slog, sure, but at the end of the day I&amp;rsquo;d have a compiler that was very nearly the same, but presumably more performant.
This attempt I gave a real try, spending maybe three nights just chugging away. What I got was just more and more errors, which was fairly disheartening.</description><content type="html"><![CDATA[<p>What if I just copied the Svelte compiler, written in TypeScript, changed all of the file extensions from <code>.ts</code> to <code>.rs</code>, and fixed all the bugs? It&rsquo;d be a slog, sure, but at the end of the day I&rsquo;d have a compiler that was very nearly the same, but presumably more performant.</p>
<p>This attempt I gave a real try, spending maybe three nights just chugging away. What I got was just more and more errors, which was fairly disheartening. I also realized just how different Rust is, and that a 1-to-1 rewrite would result in something that wouldn&rsquo;t be as ideal as it could be. For example, take this very simple TypeScript class:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-ts" data-lang="ts"><span class="kr">class</span> <span class="nx">Node</span> <span class="kr">extends</span> <span class="nx">BaseNode</span> <span class="p">{</span>
	<span class="nx">name</span>: <span class="kt">String?</span> <span class="o">=</span> <span class="kc">null</span><span class="p">,</span>
	<span class="nx">children</span>: <span class="kt">Node</span><span class="p">[]</span> <span class="o">=</span> <span class="p">[],</span>
<span class="p">}</span>
</code></pre></div><p>The equivalent Rust struct might look like:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="k">struct</span> <span class="nc">Node</span><span class="w"> </span><span class="p">{</span><span class="w">
</span><span class="w">	</span><span class="n">base</span>: <span class="nc">BaseNode</span><span class="p">,</span><span class="w">
</span><span class="w">	</span><span class="n">name</span>: <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span><span class="w">
</span><span class="w">	</span><span class="n">children</span>: <span class="nb">Vec</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;</span><span class="w">
</span><span class="w"></span><span class="p">}</span><span class="w">
</span></code></pre></div><p>I don&rsquo;t think that&rsquo;s great. What fields belong on Node? Which belong on BaseNode? Plus TypeScript&rsquo;s got a garbage collector, so for parity any <code>T</code> should probably be wrapped in an <a href="https://doc.rust-lang.org/std/rc/struct.Rc.html"><code>Rc&lt;T&gt;</code></a>. Plus everything&rsquo;s mutable, so we&rsquo;d want to wrap that in a <a href="https://doc.rust-lang.org/std/cell/struct.RefCell.html"><code>RefCell&lt;T&gt;</code></a>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="k">struct</span> <span class="nc">Node</span><span class="w"> </span><span class="p">{</span><span class="w">
</span><span class="w">	</span><span class="n">base</span>: <span class="nc">BaseNode</span><span class="p">,</span><span class="w">
</span><span class="w">	</span><span class="n">name</span>: <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span><span class="w">
</span><span class="w">	</span><span class="n">children</span>: <span class="nb">Vec</span><span class="o">&lt;</span><span class="n">Rc</span><span class="o">&lt;</span><span class="n">RefCell</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;&gt;&gt;</span><span class="w">
</span><span class="w"></span><span class="p">}</span><span class="w">
</span></code></pre></div><p>While probably fine, this just looks it&rsquo;d like a pain to work with. I think we can do better. These nodes don&rsquo;t really reference each other, so we don&rsquo;t actually need an <code>Rc</code>, and without that we wouldn&rsquo;t need a <code>RefCell</code>. Also I&rsquo;m up to <code>1337</code> errors, so I&rsquo;m pretty happy ending this attempt here.</p>
]]></content></item><item><title>Lithe 2: From Scratch</title><link>/posts/lithe-2/</link><pubDate>Mon, 03 Jan 2022 16:46:55 -0700</pubDate><guid>/posts/lithe-2/</guid><description>I&amp;rsquo;ve written a little Rust, but never professionally, so I&amp;rsquo;m sure what I write is going to be ugly to those in the know. But it compiles, which is half the battle.
Before starting this project let&amp;rsquo;s take a quick peek at the Svelte repo. Oh, huh, looks like the compiler smaller than I would have thought, weighing in at only ~800KB (if you include the runtime), but 800KB of code to rewrite is still fairly massive for a weekend warrior like myself.</description><content type="html"><![CDATA[<p>I&rsquo;ve written a little Rust, but never professionally, so I&rsquo;m sure what I write is going to be ugly to those in the know. But it compiles, which is half the battle.</p>
<p>Before starting this project let&rsquo;s take a quick peek at the Svelte repo. Oh, huh, looks like the compiler smaller than I would have thought, weighing in at only ~800KB (if you include the runtime), but 800KB of code to rewrite is still fairly massive for a weekend warrior like myself. So, for my initial attempt I crafted the world&rsquo;s smallest svelte app:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback">&lt;span&gt;Hello world!&lt;/span&gt;
&lt;span&gt;Goodbye!&lt;/span&gt;
</code></pre></div><p>I ran that through a recent Svelte compiler to get:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="cm">/* generated by Svelte v3.44.3 */</span>
<span class="kr">import</span> <span class="p">{</span>
	<span class="nx">SvelteComponent</span><span class="p">,</span>
	<span class="nx">detach</span><span class="p">,</span>
	<span class="nx">element</span><span class="p">,</span>
	<span class="nx">init</span><span class="p">,</span>
	<span class="nx">insert</span><span class="p">,</span>
	<span class="nx">noop</span><span class="p">,</span>
	<span class="nx">safe_not_equal</span><span class="p">,</span>
	<span class="nx">space</span><span class="p">,</span>
<span class="p">}</span> <span class="nx">from</span> <span class="s2">&#34;svelte/internal&#34;</span><span class="p">;</span>

<span class="kd">function</span> <span class="nx">create_fragment</span><span class="p">(</span><span class="nx">ctx</span><span class="p">)</span> <span class="p">{</span>
	<span class="kd">let</span> <span class="nx">span0</span><span class="p">;</span>
	<span class="kd">let</span> <span class="nx">t1</span><span class="p">;</span>
	<span class="kd">let</span> <span class="nx">span1</span><span class="p">;</span>

	<span class="k">return</span> <span class="p">{</span>
		<span class="nx">c</span><span class="p">()</span> <span class="p">{</span>
			<span class="nx">span0</span> <span class="o">=</span> <span class="nx">element</span><span class="p">(</span><span class="s2">&#34;span&#34;</span><span class="p">);</span>
			<span class="nx">span0</span><span class="p">.</span><span class="nx">textContent</span> <span class="o">=</span> <span class="s2">&#34;Hello world!&#34;</span><span class="p">;</span>
			<span class="nx">t1</span> <span class="o">=</span> <span class="nx">space</span><span class="p">();</span>
			<span class="nx">span1</span> <span class="o">=</span> <span class="nx">element</span><span class="p">(</span><span class="s2">&#34;span&#34;</span><span class="p">);</span>
			<span class="nx">span1</span><span class="p">.</span><span class="nx">textContent</span> <span class="o">=</span> <span class="s2">&#34;Goodbye&#34;</span><span class="p">;</span>
		<span class="p">},</span>
		<span class="nx">m</span><span class="p">(</span><span class="nx">target</span><span class="p">,</span> <span class="nx">anchor</span><span class="p">)</span> <span class="p">{</span>
			<span class="nx">insert</span><span class="p">(</span><span class="nx">target</span><span class="p">,</span> <span class="nx">span0</span><span class="p">,</span> <span class="nx">anchor</span><span class="p">);</span>
			<span class="nx">insert</span><span class="p">(</span><span class="nx">target</span><span class="p">,</span> <span class="nx">t1</span><span class="p">,</span> <span class="nx">anchor</span><span class="p">);</span>
			<span class="nx">insert</span><span class="p">(</span><span class="nx">target</span><span class="p">,</span> <span class="nx">span1</span><span class="p">,</span> <span class="nx">anchor</span><span class="p">);</span>
		<span class="p">},</span>
		<span class="nx">p</span><span class="o">:</span> <span class="nx">noop</span><span class="p">,</span>
		<span class="nx">i</span><span class="o">:</span> <span class="nx">noop</span><span class="p">,</span>
		<span class="nx">o</span><span class="o">:</span> <span class="nx">noop</span><span class="p">,</span>
		<span class="nx">d</span><span class="p">(</span><span class="nx">detaching</span><span class="p">)</span> <span class="p">{</span>
			<span class="k">if</span> <span class="p">(</span><span class="nx">detaching</span><span class="p">)</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">span0</span><span class="p">);</span>
			<span class="k">if</span> <span class="p">(</span><span class="nx">detaching</span><span class="p">)</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">t1</span><span class="p">);</span>
			<span class="k">if</span> <span class="p">(</span><span class="nx">detaching</span><span class="p">)</span> <span class="nx">detach</span><span class="p">(</span><span class="nx">span1</span><span class="p">);</span>
		<span class="p">},</span>
	<span class="p">};</span>
<span class="p">}</span>

<span class="kr">class</span> <span class="nx">Component</span> <span class="kr">extends</span> <span class="nx">SvelteComponent</span> <span class="p">{</span>
	<span class="nx">constructor</span><span class="p">(</span><span class="nx">options</span><span class="p">)</span> <span class="p">{</span>
		<span class="kr">super</span><span class="p">();</span>
		<span class="nx">init</span><span class="p">(</span><span class="k">this</span><span class="p">,</span> <span class="nx">options</span><span class="p">,</span> <span class="kc">null</span><span class="p">,</span> <span class="nx">create_fragment</span><span class="p">,</span> <span class="nx">safe_not_equal</span><span class="p">,</span> <span class="p">{});</span>
	<span class="p">}</span>
<span class="p">}</span>
</code></pre></div><p>Alright, seems simple enough. What if I just write a very very very basic compiler that parses the DOM tree looking for Elements and then spits out the resultant JavaScript as it chugs along? If we merge all the compilation steps we avoid running through the file multiple times, a plus for performance. So I gave it a shot, and got something that produced exactly the same output, but only for the simplest cases. After, I realized that, while this approach would yield something very very fast, doing everything in one step made the program fairly difficult to change. I think I&rsquo;d be better off sacrificing just a little performance in exchange for modularity. Let&rsquo;s shelve this attempt for now and try something more traditional.</p>
]]></content></item><item><title>Lithe 1: The Motivation</title><link>/posts/lithe-1/</link><pubDate>Mon, 03 Jan 2022 16:12:04 -0700</pubDate><guid>/posts/lithe-1/</guid><description>Svelte is like Rust for me in that I had heard good things for years before I even gave it a try.
Svelte is like Rust for me in that I loved it immediately.
It&amp;rsquo;s really very simple to write in, the basic idea is engaging, and after trying it I wanted so badly for it to succeed. For context I have professional experience with vanilla JavaScript, React, and Angular. After transitioning a small app from Angular to Svelte, here were my takeaways:</description><content type="html"><![CDATA[<p>Svelte is like Rust for me in that I had heard good things for years before I even gave it a try.</p>
<p>Svelte is like Rust for me in that I loved it immediately.</p>
<p>It&rsquo;s really very simple to write in, the basic idea is engaging, and after trying it I wanted so badly for it to succeed. For context I have professional experience with vanilla JavaScript, React, and Angular. After transitioning a small app from Angular to Svelte, here were my takeaways:</p>
<ol>
<li>It&rsquo;s very much like Vue
<ul>
<li>Single file structure</li>
<li>Framework provides a lot out of the box</li>
</ul>
</li>
<li>It&rsquo;s got a better pattern for reactive programming than React
<ul>
<li>I&rsquo;m guessing Svelte is a large part of the motivation behind React&rsquo;s new <a href="https://www.youtube.com/watch?v=lGEMwh32soc">React without memo</a> project</li>
</ul>
</li>
<li>It&rsquo;s had five years to become popular, and yet it has not.
<ul>
<li>I can only guess at this, but I&rsquo;d say Svelte was simply late to the game. So much of the web is written using other frameworks, and no one wants to re-write it all</li>
<li>Initially Svelte touted itself as providing all the functionality you&rsquo;d expect from a big JS framework with way less code. That&rsquo;s turned out to not be the <a href="https://github.com/sveltejs/svelte/issues/2546#issuecomment-678845774">full story</a>, and an app with lots of components will end up being less LOC if written in, say, React than Svelte</li>
</ul>
</li>
</ol>
<p>To appease my most recent hyperfixation I was listening to anything with Rich Harris (the creator of Svelte) in it. One such piece of media was <a href="https://changelog.com/jsparty/205">JS Party 205</a>. In it Rich had a throwaway line about maybe someday rewriting the compiler in Rust. It came across as a &ldquo;it&rsquo;d be nice to have, but it&rsquo;s definitely not a priority&rdquo; project. Which makes sense. The Svelte compiler is already mostly TypeScript, so it&rsquo;s got enough static typing to be safe and is also very easy for new people to contribute to, a must for any open source project. Rich is wise to stick to improving the Developer Experience first before doing a largely unnecessary rewrite. React is proof that if your JS framework makes it quick to develop apps people will use it regardless of bundle size.</p>
<p>But that doesn&rsquo;t stop this project from being very tempting to take on, and it definitely doesn&rsquo;t stop me from trying. So this is going to be the start of a new series in which I chronicle my journey of rewriting a compiler (which I&rsquo;ve never done before) for a project I&rsquo;ve barely used. What I&rsquo;m hoping to get out of it:</p>
<ol>
<li>A better understanding of:
<ul>
<li>Svelte</li>
<li>JavaScript</li>
<li>Compilers</li>
<li>Rust</li>
<li>Tackling big projects</li>
</ul>
</li>
<li>A compiler that can do a significant subset of what the Svelte compiler can do, but much much faster
<ul>
<li>I don&rsquo;t yet care for Server-Side Rendering, so that should cut the work a bit</li>
</ul>
</li>
<li>Smaller bundle sizes
<ul>
<li>Code splitting can only do so much</li>
<li>Not caring about bundle sizes is how we end up with 5MB of JS</li>
</ul>
</li>
<li>A list of changes I&rsquo;d like to make to the Svelte compiler so even if my compiler doesn&rsquo;t get used the community can still benefit
<ul>
<li>Can we decrease bundle size?</li>
<li>Can we speed up compilation?</li>
<li>Are there any edge-cases not currently handled well?</li>
</ul>
</li>
</ol>
]]></content></item><item><title>Site V2</title><link>/posts/site-v2/</link><pubDate>Mon, 27 Dec 2021 14:15:48 -0700</pubDate><guid>/posts/site-v2/</guid><description>Channeling my inner dev, I&amp;rsquo;ve gone ahead and completely re-written this site. Original post for comparison.
Why? The original site was&amp;hellip; poorly written. It used React, which can be fine, but was overkill for a site that wasn&amp;rsquo;t particularly complex interactive. And I&amp;rsquo;ve had a change of heart when it comes to SPAs: that they&amp;rsquo;re useful when they&amp;rsquo;re useful and not when they&amp;rsquo;re not.
Free Hosting this site is still basically free via Github pages (except for the domain name).</description><content type="html"><![CDATA[<p>Channeling my inner dev, I&rsquo;ve gone ahead and completely re-written this site. <a href="https://loremdipso.com/posts/conception/">Original post for comparison</a>.</p>
<h2 id="why">Why?</h2>
<p>The original site was&hellip; poorly written. It used React, which can be fine, but was overkill for a site that wasn&rsquo;t particularly complex interactive. And I&rsquo;ve had a change of heart when it comes to SPAs: that they&rsquo;re useful when they&rsquo;re useful and not when they&rsquo;re not.</p>
<h2 id="free">Free</h2>
<p>Hosting this site is still basically free via Github pages (except for the domain name).</p>
<h2 id="fast">Fast</h2>
<p>I switched from React and webpack to Hugo and I couldn&rsquo;t be happier. My build times are basically <a href="https://gohugo.io/troubleshooting/build-performance/">instant now</a>, and the site is generally faster for users to load and to render. I may revisit turning this generate site into a SPA again, but it&rsquo;d just be a vanity project. Each page&rsquo;s chrome is so simple I wouldn&rsquo;t actually save any real time by keeping it around and swapping out the content.</p>
<h2 id="fun">Fun</h2>
<p>Eh, Hugo is great, but it&rsquo;s also opinionated and a little difficult to grok at first if you haven&rsquo;t worked with it before. Plus I&rsquo;ve found the online forum moderators to be a little short-tempered. Overall I&rsquo;m happy with my choice.</p>
<h2 id="fanciful">Fanciful</h2>
<p>I added nifty slide-in effects for the header/footer and a fade-in effect for the content to make the site just a little bit more interesting. The moving backgrounds are gone, though, for now (☹️). I may add them back in a bit, maybe as part of a shared library. I&rsquo;m imagining you import some CSS file and it handles the rest, but idk.</p>
]]></content></item><item><title>Finding My Voice</title><link>/posts/finding-my-voice/</link><pubDate>Mon, 27 Dec 2021 13:33:48 -0700</pubDate><guid>/posts/finding-my-voice/</guid><description>I started this site for the same reason I imagine most software developers do: to get hired. Looking back, I&amp;rsquo;m not really surprised by the result: I was a perfectionist, it took a very long time (and a few tries), and it did not help me get hired. So, with that, and the face that I&amp;rsquo;ve been gainfully employed for almost a year now, why resurrect this old thing?
Well, I did spend an awful lot of time on this thing.</description><content type="html"><![CDATA[<p>I started this site for the same reason I imagine most software developers do: to get hired. Looking back, I&rsquo;m not really surprised by the result: I was a perfectionist, it took a very long time (and a few tries), and it did not help me get hired. So, with that, and the face that I&rsquo;ve been gainfully employed for almost a year now, why resurrect this old thing?</p>
<p>Well, I did spend an awful lot of time on this thing. Might as well use it, eh? Now, with that decided, what kind of content do I want to put out there? Well let&rsquo;s do what we know, which isn&rsquo;t much. The shortlist:</p>
<ul>
<li>Snarky technical (a la Coding Horror).</li>
<li>Dry technical (a la Rust Docs).</li>
<li>Amateur scifi</li>
<li>Amateur comedy</li>
</ul>
<p>Let&rsquo;s try some of these out and see what sticks.</p>
]]></content></item><item><title>Conception</title><link>/posts/conception/</link><pubDate>Mon, 10 Aug 2020 13:08:19 -0700</pubDate><guid>/posts/conception/</guid><description>This site was made, primarily, as a vehicle for my résumé. It could have been a one-day thing. Just find a pretty wordpress template, customize it a bit, and voilà! But where&amp;rsquo;s the fun in that? I mean, what self-respecting dev doesn&amp;rsquo;t love a good greenfield project.
So, to justify this waste of time, I came up with some requirements:
Free This was by far the easiest task since github will host a page for your user.</description><content type="html"><![CDATA[<p>This site was made, primarily, as a vehicle for my résumé. It could have been a one-day thing. Just find a pretty wordpress template, customize it a bit, and voilà! But where&rsquo;s the fun in that? I mean, what self-respecting dev doesn&rsquo;t love a good <a href="https://en.wikipedia.org/wiki/Greenfield_project">greenfield project</a>.</p>
<p>So, to justify this waste of time, I came up with some requirements:</p>
<h2 id="free">Free</h2>
<p>This was by far the easiest task since github will host a page for your user. This only works for static sites (no SSR), but that&rsquo;s my preference anyhow. I did still have to pay for the domain name, though.</p>
<h2 id="fast">Fast</h2>
<p>Fast means different things to different people. I knew this site was going to be mostly static content, and my hoster already had CDNs. The question was: do I bother making a <a href="https://en.wikipedia.org/wiki/Single-page_application">SPA</a>? On the one hand, I think you can do some pretty cool things with a SPA. On the other, it&rsquo;s definitely more work. Could I really justify the time in this already overly-onerous task?</p>
<p>But we&rsquo;re greenfielding it, so SPA it is! And I chose gatsby to help out with navigation and also generating the blog content. For the résumé I have kind of an exaggerated pipeline, where I fill out a yml which I convert to json and feed into a page on this site. To get a <code>pdf</code> I wrote a script that effectively hosts the page in a headless browser and prints the result to a file. To get a <code>.docx</code> I&hellip; would have to write it by hand. But luckily <code>.docx</code> is far less common.</p>
<p>The skeleton of the site is loaded regardless of which page you go to. Heavier projects will be <a href="https://reactjs.org/docs/code-splitting.html">split out and lazy loaded</a>, or else hosted separately, so the site is about as fast as it can reasonably be and shouldn&rsquo;t get slower even as I add content.</p>
<h2 id="fun">Fun</h2>
<p>I chose React as the framework, since I know it and really enjoy using it. It was pretty overkill for this project since not a lot of this site it reactive, but, like Shane Patton is credited as saying: anything worth doing is worth overdoing.</p>
<h2 id="fanciful">Fanciful</h2>
<p>I love moving backgrounds. I flip-flopped on how best to accomplish this, CSS or canvas. CSS, especially CSS3, has gotten so good and feature rich that people have done all sorts of amazing things. All sorts of problems, like jitter and framerate, would go away. But while it would be easier to get something up and running, just CSS is more limiting than the canvas approach, so that&rsquo;s what I went with.</p>
<p>The first iteration is a simple collage of circles that lazily drift around, growing and shrinking at random. It&rsquo;s not perfect, but pretty enough for a first go.</p>
<h1 id="conclusion">Conclusion</h1>
<p>And that&rsquo;s it. I hope you enjoy this site as much as I&rsquo;ve enjoyed making it. Ciao!</p>
]]></content></item><item><title>What's with the name?</title><link>/posts/whats-with-the-name/</link><pubDate>Mon, 04 May 2020 13:08:19 -0700</pubDate><guid>/posts/whats-with-the-name/</guid><description>&amp;ldquo;loremdipso&amp;rdquo;? What&amp;rsquo;s that? Did you mean lorem ipsum? Or, ipso facto maybe?
Ah, well, let me take you back to the summer of 1992, when I was born. The story I&amp;rsquo;ve been told is that my sister wanted to name me after that kid on the Kix commercial. You know, the one that goes &amp;ldquo;Mikey likes it!&amp;quot;1. Pretty cute, right? Named by my sister, who was only 6 or 7, after a silly cereal commercial.</description><content type="html"><![CDATA[<p>&ldquo;loremdipso&rdquo;? What&rsquo;s that? Did you mean <a href="https://loremipsum.io/">lorem ipsum</a>? Or, <a href="https://www.merriam-webster.com/dictionary/ipso%20facto">ipso facto</a> maybe?</p>
<p>Ah, well, let me take you back to the summer of 1992, when I was born. The story I&rsquo;ve been told is that my sister wanted to name me after that kid on the Kix commercial. You know, the one that goes <a href="https://www.youtube.com/watch?v=vYEXzx-TINc">&ldquo;Mikey likes it!&quot;</a><sup id="fnref:1"><a href="#fn:1" class="footnote-ref" role="doc-noteref">1</a></sup>. Pretty cute, right? Named by my sister, who was only 6 or 7, after a silly cereal commercial.</p>
<p>The problem is that Mikey (or Michael, as I was actually named), was <a href="https://www.ssa.gov/oact/babynames/decades/names1990s.html">by far</a> the most popular boys name in the US in the 90s. And Adams, my surname as well as that of two former presidents<sup id="fnref:2"><a href="#fn:2" class="footnote-ref" role="doc-noteref">2</a></sup>, isn&rsquo;t exactly unique.</p>
<p>So, here we are, 2020, the age of the machine. I tried valiantly and often, but ultimately every good handle containing any combination of &ldquo;Michael&rdquo; and &ldquo;Adams&rdquo; had already been thought of and taken. From Twitter to email to web domains, taken. What was I to do?</p>
<p>So, I treated my problem like a minimal viable product and just did it as best I knew how in as short a time as possible. Was combining a phrase vaguely related to tech with a latin one that kind of sounded similar to produce a pretty unrecognizable portmanteau the best choice? Probably not. But sometimes the best choice is to make <em>a</em> choice and just move on with your life. The time I could have spent thinking up new names or attempting to buy existing names off their owners is did not promise an adequate ROI.</p>
<section class="footnotes" role="doc-endnotes">
<hr>
<ol>
<li id="fn:1" role="doc-endnote">
<p>The premise of that commercial is, I think, fundamentally flawed. The other children, dubious about this new cereal, decide to give it to Mikey because he &ldquo;won&rsquo;t eat it&rdquo; / &ldquo;hates everything&rdquo;. What was their goal? If he hates everything then he&rsquo;ll hate it and then they&rsquo;ve accomplished nothing. It&rsquo;s like a fever dream, where it kind of seems to make sense until you pick it apart in the ruddy light of day.&#160;<a href="#fnref:1" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
<li id="fn:2" role="doc-endnote">
<p>The only repeated surnames among US presidents, so far, are: Adams, Bush, Harrison, Johnson, and Roosevelt&#160;<a href="#fnref:2" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
</ol>
</section>
]]></content></item></channel></rss>